{{- if and .Values.config.enabled .Values.config.data }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: spfa-config
  labels:
    app: spfa
data:
  {{- toYaml .Values.config.data | nindent 2 }}
{{- end }}
