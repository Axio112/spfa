apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "spfa.fullname" . }}-config
  labels:
    {{- include "spfa.labels" . | nindent 4 }}
data:
{{- range $k, $v := .Values.env }}
  {{ $k }}: {{ $v | quote }}
{{- end }}
